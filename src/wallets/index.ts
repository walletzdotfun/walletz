import { PhantomAdapter } from './phantom';
import { SolflareAdapter } from './solflare';
import { GlowAdapter } from './glow';
import { BackpackAdapter } from './backpack';

export const ALL_WALLETS = [PhantomAdapter, SolflareAdapter, GlowAdapter, BackpackAdapter];
